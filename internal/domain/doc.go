// Package domain contains value types only: structs, enums, and sentinel errors.
// No interfaces, no external imports, no infrastructure dependencies.
// Every other package may import domain; domain imports nothing.
package domain
